//cout << "0";
/*memset(recvbuff, 0, totalsize);
cout << "123";
recvfrom(sockServer, recvbuff, totalsize, 0, (SOCKADDR*)&ClintAddr, &fromlen);
cout << "123456789" << endl;
//sendto(sockServer, nak, 5, 0, (SOCKADDR*)&ClintAddr, sizeof(SOCKADDR));
cout << "123";
if (check(recvbuff))
{
	cout << "error";
	nak[4] = recvbuff[251];
}

s = recvbuff;
if (s == "end")
{
	break;
}
else if (s == "SYN")
{
	memset(recvbuff, 0, totalsize);
	ack[4] = bit;
	//
	cout << "sendsyn" << endl;
}
else
{
	fout.write(recvbuff, sizeof(char) * SEND_A_TIME);
}



		} while (!(s == "end"));
		cout << "end";
		fout.close();
		cout << "over";
		getchar();*/